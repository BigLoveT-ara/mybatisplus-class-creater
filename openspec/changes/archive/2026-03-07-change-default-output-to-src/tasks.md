## 1. 修改默认输出路径

- [x] 1.1 修改 `cli.ts` 中 `generate` 命令的 `--output` 选项默认值从 `./generated` 改为 `./`

## 2. 验证

- [x] 2.1 重新编译项目 `npm run build`
- [x] 2.2 测试不传 `-o` 参数时代码生成到 `./` 目录
- [x] 2.3 测试传入 `-o` 参数时能正常覆盖默认行为

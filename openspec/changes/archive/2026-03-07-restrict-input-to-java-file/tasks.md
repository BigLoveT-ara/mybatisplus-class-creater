## 1. 修改 cli.ts 中的 generate 命令

- [x] 1.1 在 action 中判断输入路径是文件还是目录（使用 `fs.statSync`）
- [x] 1.2 如果是文件，验证后缀是否为 `.java`，否则报错退出
- [x] 1.3 如果是文件，直接解析该文件获取 `JavaClass` 对象
- [x] 1.4 如果是目录，保持原有扫描逻辑不变

## 2. 验证

- [x] 2.1 重新编译项目 `npm run build`
- [x] 2.2 测试传入单个 `.java` 文件能正确生成代码
- [x] 2.3 测试传入目录能正确生成所有实体的代码
- [x] 2.4 测试传入非 `.java` 文件能正确报错
